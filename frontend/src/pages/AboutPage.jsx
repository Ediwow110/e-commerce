import React from 'react';
import { SectionTitle } from '../components/SectionTitle.jsx';

export function AboutPage() {
  return (
    <section className="section-pad page editorial">
      <SectionTitle eyebrow="Brand Story" title="Crafted for timeless everyday luxury." />
      <p>LUXE is a frontend concept for boutiques that need a premium visual experience, trust-driven product pages, and polished browsing flows.</p>
    </section>
  );
}
