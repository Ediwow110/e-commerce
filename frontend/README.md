# LUXE Commerce Frontend

React + Vite premium UI for jewelry and bags e-commerce.

```bash
npm install
npm run dev
```

Set API URL:

```bash
cp .env.example .env
```

The frontend is still UI-first, but it is now ready to connect to the backend API included in this full-stack package.
